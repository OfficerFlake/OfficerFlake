<UserControl x:Class="Com.OfficerFlake.Libraries.UserInterfaces.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d" 
             d:DesignWidth="640" d:DesignHeight="24">
	<Border Grid.Row="0" Background="#eeeeee">
		<Menu Name="MenuBarViewModel"
		      VerticalAlignment="Center"
		      BorderThickness="0">

			<MenuItem Header="OpenYS">
				<MenuItem Header="Console Output">
					<MenuItem Header="Show Messages...">
						<MenuItem Header="Console" IsCheckable="True" IsChecked="{Binding OpenYSServerModeUserInterface.ShowMessages_Console, Mode=TwoWay}"/>
						<MenuItem Header="Users" IsCheckable="True" IsChecked="True"/>
					</MenuItem>
					<MenuItem Header="Show Debug...">
						<MenuItem Header="Summary" IsCheckable="True" IsChecked="False"/>
						<MenuItem Header="Detail" IsCheckable="True" IsChecked="False"/>
						<MenuItem Header="Warning" IsCheckable="True" IsChecked="True"/>
						<MenuItem Header="Error" IsCheckable="True" IsChecked="True"/>
						<MenuItem Header="Crash" IsCheckable="True" IsChecked="True"/>
					</MenuItem>
				</MenuItem>
			</MenuItem>

		</Menu>
	</Border>
</UserControl>
